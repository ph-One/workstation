---
- hosts: localhost
  tasks:
  - name: install packages
    package:
      name: "{{ item }}"
      state: present
    with_items:
    - tmux
    - zsh
    - joplin-desktop
    - vivaldi
    - fish-shell
    - guake
    - podman
    - cowsay
