---
- hosts: localhost
  become: true
  tasks:
  - name: install packages
    command: paru -S --skipreview {{ item }}
    with_items:
    - tmux
    - zsh
    - joplin-desktop
    - vivaldi
    - fish-git
    - guake
    - podman
    - cowsay
